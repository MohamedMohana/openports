name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build and Package
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - package_arch: arm64
            swift_arch: arm64
          - package_arch: intel
            swift_arch: x86_64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Swift Version
        run: swift --version

      - name: Resolve Version
        id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Build Release
        run: swift build -c release --arch "${{ matrix.swift_arch }}"

      - name: Package App
        run: |
          chmod +x Scripts/package_app.sh
          ./Scripts/package_app.sh release "${{ matrix.package_arch }}"

      - name: Archive App
        run: |
          zip -r "OpenPorts-${{ steps.version.outputs.tag }}-${{ matrix.package_arch }}.zip" OpenPorts.app
          shasum -a 256 "OpenPorts-${{ steps.version.outputs.tag }}-${{ matrix.package_arch }}.zip" > "OpenPorts-${{ steps.version.outputs.tag }}-${{ matrix.package_arch }}.sha256"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: OpenPorts-${{ matrix.package_arch }}
          path: |
            OpenPorts-${{ steps.version.outputs.tag }}-${{ matrix.package_arch }}.zip
            OpenPorts-${{ steps.version.outputs.tag }}-${{ matrix.package_arch }}.sha256
          retention-days: 3

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Resolve Version
        id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Download ARM64 Artifact
        uses: actions/download-artifact@v4
        with:
          name: OpenPorts-arm64
          path: artifacts

      - name: Download Intel Artifact
        uses: actions/download-artifact@v4
        with:
          name: OpenPorts-intel
          path: artifacts

      - name: Validate Artifacts
        run: |
          ls -lh artifacts
          test -f "artifacts/OpenPorts-${{ steps.version.outputs.tag }}-arm64.zip"
          test -f "artifacts/OpenPorts-${{ steps.version.outputs.tag }}-intel.zip"
          test -f "artifacts/OpenPorts-${{ steps.version.outputs.tag }}-arm64.sha256"
          test -f "artifacts/OpenPorts-${{ steps.version.outputs.tag }}-intel.sha256"
          cat artifacts/*.sha256

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: OpenPorts v${{ steps.version.outputs.version }}
          generate_release_notes: true
          prerelease: ${{ contains(steps.version.outputs.tag, '-') }}
          files: |
            artifacts/OpenPorts-${{ steps.version.outputs.tag }}-arm64.zip
            artifacts/OpenPorts-${{ steps.version.outputs.tag }}-intel.zip
            artifacts/OpenPorts-${{ steps.version.outputs.tag }}-arm64.sha256
            artifacts/OpenPorts-${{ steps.version.outputs.tag }}-intel.sha256

  update-tap:
    name: Update Homebrew Tap
    needs: release
    runs-on: ubuntu-latest
    if: github.repository == 'MohamedMohana/openports'
    env:
      TAP_GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Resolve Version
        id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Skip Tap Update Without GH_TOKEN
        if: env.TAP_GH_TOKEN == ''
        run: echo "GH_TOKEN is not configured; skipping Homebrew tap update."

      - name: Download Artifacts
        if: env.TAP_GH_TOKEN != ''
        uses: actions/download-artifact@v4
        with:
          pattern: OpenPorts-*
          path: artifacts
          merge-multiple: true

      - name: Extract Checksums
        if: env.TAP_GH_TOKEN != ''
        id: sha
        run: |
          ARM64_SHA=$(awk '{print $1}' artifacts/*-arm64.sha256)
          INTEL_SHA=$(awk '{print $1}' artifacts/*-intel.sha256)
          echo "arm64=${ARM64_SHA}" >> "$GITHUB_OUTPUT"
          echo "intel=${INTEL_SHA}" >> "$GITHUB_OUTPUT"

      - name: Checkout Tap Repo
        if: env.TAP_GH_TOKEN != ''
        uses: actions/checkout@v4
        with:
          repository: MohamedMohana/homebrew-tap
          token: ${{ env.TAP_GH_TOKEN }}
          path: tap

      - name: Update Cask
        if: env.TAP_GH_TOKEN != ''
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          VERSION="${{ steps.version.outputs.version }}"
          ARM64_SHA="${{ steps.sha.outputs.arm64 }}"
          INTEL_SHA="${{ steps.sha.outputs.intel }}"

          cat > tap/Casks/openports.rb <<EOF
          cask "openports" do
            version "${VERSION}"

            on_arm do
              url "https://github.com/MohamedMohana/openports/releases/download/${TAG}/OpenPorts-${TAG}-arm64.zip",
                  verified: "github.com/MohamedMohana/openports/"
              sha256 "${ARM64_SHA}"
            end

            on_intel do
              url "https://github.com/MohamedMohana/openports/releases/download/${TAG}/OpenPorts-${TAG}-intel.zip",
                  verified: "github.com/MohamedMohana/openports/"
              sha256 "${INTEL_SHA}"
            end

            name "OpenPorts"
            desc "Smart port monitoring for Mac developers"
            homepage "https://github.com/MohamedMohana/openports"

            auto_updates false
            depends_on macos: ">= :sonoma"

            app "OpenPorts.app"
          end
          EOF

      - name: Commit and Push Tap Changes
        if: env.TAP_GH_TOKEN != ''
        run: |
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/openports.rb
          git commit -m "Update openports to ${{ steps.version.outputs.version }}" || exit 0
          git push
